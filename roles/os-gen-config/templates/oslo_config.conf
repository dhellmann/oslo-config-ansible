# Generated by os-config-gen

{% macro emit_option(group_name, group, opt) -%}
{%- set group_values = oslo_config.get(group_name, {}) -%}
{% if oslo_config_show_defaults or 
    (opt.name in group_values and opt.default != group_values.get(opt.name)) -%}
{% if oslo_config_show_help -%}
# {{ opt.help|wordwrap(79, False, '\n# ') }}
{% endif %}
{{opt.name}} = {{group_values[opt.name]|default(opt.default)}}
{% endif %}
{%- endmacro %}

{% for group_name, group in os_gen_config_schema['options']|dictsort -%}
[{{group_name}}]
{% for opt in group.opts -%}
{% if not group.driver_option -%}{# there is no driver option, so emit the option #}
{{emit_option(group_name, group, opt)}}
{%- endif %}
{%- endfor %}{# opts within a group #}
{%- endfor %}{# groups #}